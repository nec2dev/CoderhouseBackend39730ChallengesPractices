/* Template strings:
    ✓Funcionan como un supersetde una string.
    ✓ Permite incrustarinformación dentro de ella, evitando la concatenación.
    ✓ Reconoce los saltos de línea, para mantener el formato.
    ✓ Pero más importante aún es que no presenta el límite de caracteres de 
    una string normal (Permitiendo hacer estructuras más complejas, 
    como plantillas)*/

    `texto de cadena de caracteres`

    `linea 1 de la cadena de caracteres
     linea 2 de la cadena de caracteres`

    `texto de cadena de caracteres ${variable} texto de cadena de caracteres`

    `texto de cadena de caracteres ${expresion} texto adicional de cadena de caracteres`